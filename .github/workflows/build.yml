name: Compile

on:
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows-x86_64
            os: windows-2022
            suffix: -x86_64.exe

          - name: macos-intel
            os: macos-15-intel
            suffix: -macOS-intel

          - name: macos-arm
            os: macos-latest
            suffix: -macOS-arm

          - name: linux-x86_64
            os: ubuntu-latest
            suffix: -linux

          - name: linux-arm
            os: ubuntu-24.04-arm
            suffix: -linux-arm

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Setup Fortran
        uses: fortran-lang/setup-fortran@v1.9.0
        with:
          compiler: gcc
          version: latest

      - name: Build
        shell: bash
        run: |
          mkdir -p dist

          if [[ "${{ matrix.suffix }}" == *".exe"* ]]; then
            "${FC:-gfortran}" -static -Wall -Wextra -O2 main.f90 -o "dist/tictactoe${{ matrix.suffix }}"
          else
            "${FC:-gfortran}" -Wall -Wextra -O2 main.f90 -o "dist/tictactoe${{ matrix.suffix }}"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: tictactoe-${{ matrix.name }}
          path: dist/tictactoe${{ matrix.suffix }}